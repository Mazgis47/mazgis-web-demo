apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: demo-website
  name: demo-website
spec:
  host: demo-website.apps.az2-osp00.mazgis47.net
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Allow
    termination: edge
  to:
    kind: Service
    name: demo-website-http
    weight: 100
  wildcardPolicy: None
